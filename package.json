{
  "name": "gsplay",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo '⚠️  SAFETY CHECK: Ensure NODE_ENV=test and MongoDB URI points to localhost before running tests!' && jest",
    "test:watch": "echo '⚠️  SAFETY CHECK: Ensure NODE_ENV=test and MongoDB URI points to localhost before running tests!' && jest --watch",
    "test:coverage": "echo '⚠️  SAFETY CHECK: Ensure NODE_ENV=test and MongoDB URI points to localhost before running tests!' && jest --coverage",
    "test:unit": "echo '⚠️  SAFETY CHECK: Ensure NODE_ENV=test and MongoDB URI points to localhost before running tests!' && jest tests/unit",
    "test:integration": "echo '⚠️  SAFETY CHECK: Ensure NODE_ENV=test and MongoDB URI points to localhost before running tests!' && jest tests/integration",
    "test:api": "echo '⚠️  SAFETY CHECK: Ensure NODE_ENV=test and MongoDB URI points to localhost before running tests!' && jest tests/api",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "start:v2": "node src/v2/server.js",
    "worker:v2": "node src/v2/worker.js",
    "bootstrap:v2": "node scripts/bootstrap-v2-schema.js",
    "migrate:v1-to-v2": "node scripts/migrate-v1-to-v2.js",
    "test:v2": "npm test -- --runInBand tests/v2",
    "docs": "jsdoc -c jsdoc.conf.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@retroachievements/api": "^2.7.0",
    "axios": "^1.12.2",
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "debug": "^4.4.3",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "express-session": "^1.18.2",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.3",
    "multer": "^2.0.2",
    "steamapi": "^3.1.3"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "jsdoc": "^4.0.4",
    "mongodb-memory-server": "^10.2.1",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.4"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ],
    "collectCoverageFrom": [
      "src/**/*.js",
      "!src/**/*.test.js",
      "!src/**/index.js"
    ],
    "coverageDirectory": "tests/coverage",
    "coverageReporters": [
      "text",
      "lcov",
      "html"
    ],
    "setupFilesAfterEnv": [
      "<rootDir>/tests/setup.js"
    ]
  }
}
