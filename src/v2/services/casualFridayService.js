const CanonicalGame = require('../models/CanonicalGame'); const Rotation = require('../models/CasualFridayRotationGame'); const Playlist = require('../models/CasualFridayPlaylist'); const Entry = require('../models/CasualFridayPlaylistEntry'); const Audit = require('../models/CasualFridayAudit'); const LibraryItem = require('../models/LibraryItem'); const { normalizeTitle } = require('./titleNormalization'); const { ItadProviderError } = require('../providers/itadClient'); const { AppError } = require('../http/errors');
const cleanDisplayTitle = (value) => String(value || '').trim().replace(/\s+-\s+[^\s]+\.(?:exe|app|bat|cmd|sh)$/i, '').trim() || String(value || '').trim(); const modes = new Set(['none', 'host_runs', 'streamable']); const kinds = new Set(['owned_store', 'external_store', 'free', 'web']); const https = (value, field) => { if (!/^https:\/\//.test(value || '')) throw new AppError(400, 'invalid_request', `${field} must be an HTTPS URL`); };
async function audit(actor, kind, data) { await Audit.create({ actorUserId: actor._id, actorUsernameSnapshot: actor.usernameDisplay, kind, ...data }); } const playable = (rotation) => ['verified', 'not_required'].includes(rotation.itadStatus); const gameDto = (game) => ({ id: String(game._id), title: game.canonicalTitle, artwork: game.artwork, summary: game.summary, genres: game.genres || [], igdbUrl: game.igdbUrl });
async function verifyItad(rotation, itadClient) { if (['free', 'web'].includes(rotation.acquisitionKind)) { Object.assign(rotation, { itadStatus: 'not_required', itadCheckedAt: new Date(), itadError: undefined, itadGameId: undefined }); return; } try { const found = await itadClient.lookupTitle(rotation.displayTitle); Object.assign(rotation, { itadCheckedAt: new Date(), itadStatus: found.outcome === 'matched' ? 'verified' : found.outcome === 'ambiguous' ? 'ambiguous' : 'not_found', itadGameId: found.outcome === 'matched' ? found.game.id : undefined, itadTitle: found.outcome === 'matched' ? found.game.title : undefined, itadError: undefined }); } catch (error) { Object.assign(rotation, { itadStatus: 'error', itadCheckedAt: new Date(), itadError: error instanceof ItadProviderError ? error.message : 'ITAD verification failed' }); } }
function fields(data) { const value = { ...data, displayTitle: cleanDisplayTitle(data.displayTitle) }; if (!value.displayTitle || !Number.isInteger(value.playerCountMin) || !Number.isInteger(value.playerCountMax) || value.playerCountMin < 1 || value.playerCountMax < value.playerCountMin || !modes.has(value.hostMode) || !kinds.has(value.acquisitionKind)) throw new AppError(400, 'invalid_request', 'Rotation fields are invalid'); if (value.artworkOverride) https(value.artworkOverride, 'artworkOverride'); if (['free', 'web', 'external_store'].includes(value.acquisitionKind)) https(value.acquisitionUrl, 'acquisitionUrl'); return value; }
const rotationDto = (rotation, game, timesPlayed = 0, offer = null, inUpcomingPlaylist = false) => ({ id: String(rotation._id), canonicalGameId: String(rotation.canonicalGameId), displayTitle: cleanDisplayTitle(rotation.displayTitle || game?.canonicalTitle || 'Untitled game'), artwork: rotation.artworkOverride || game?.artwork || null, info: rotation.info || game?.summary || '', status: rotation.status, playerCountMin: rotation.playerCountMin, playerCountMax: rotation.playerCountMax, playerCountLabel: rotation.playerCountLabel, joinInstructions: rotation.joinInstructions, hostMode: rotation.hostMode, acquisitionKind: rotation.acquisitionKind, acquisitionUrl: rotation.acquisitionUrl, availabilityNote: rotation.availabilityNote, itad: { status: rotation.itadStatus, gameId: rotation.itadGameId, title: rotation.itadTitle, checkedAt: rotation.itadCheckedAt, error: rotation.itadError, offer }, playable: playable(rotation), inUpcomingPlaylist, timesPlayed, game: game && gameDto(game) });
async function listRotation({ itadClient, includeOffer = false } = {}) { const rotations = await Rotation.find().sort({ status: 1, createdAt: -1 }); const games = await CanonicalGame.find({ _id: { $in: rotations.map((item) => item.canonicalGameId) } }); const gameMap = new Map(games.map((game) => [String(game._id), game])); return Promise.all(rotations.map(async (rotation) => { let offer = null; if (includeOffer && rotation.itadStatus === 'verified') try { offer = await itadClient.bestOffer(rotation.itadGameId); } catch {} return rotationDto(rotation, gameMap.get(String(rotation.canonicalGameId)), 0, offer); })); }
async function createRotation(actor, data, { itadClient } = {}) { const game = await CanonicalGame.findOne({ _id: data.canonicalGameId, hiddenAt: null, archivedAt: null, mergedIntoId: null }); if (!game) throw new AppError(404, 'not_found', 'Catalogue game was not found'); if (await Rotation.exists({ canonicalGameId: game._id, status: 'active' })) throw new AppError(409, 'rotation_game_exists', 'Game is already in the active rotation'); const rotation = new Rotation({ ...fields({ ...data, displayTitle: data.displayTitle || cleanDisplayTitle(game.canonicalTitle), info: data.info ?? game.summary }), canonicalGameId: game._id, addedBy: actor._id, updatedBy: actor._id }); await verifyItad(rotation, itadClient); await rotation.save(); await audit(actor, 'rotation_added', { rotationGameId: rotation._id }); return rotationDto(rotation, game); }
async function createExternalRotation(actor, data, { igdbClient, itadClient } = {}) { let game; if (data.igdbSlug) { const metadata = await igdbClient.getGameBySlug(data.igdbSlug); if (!metadata) throw new AppError(404, 'not_found', 'IGDB game was not found'); game = await CanonicalGame.findOne({ igdbId: metadata.igdbId, mergedIntoId: null }) || await CanonicalGame.create({ ...metadata, origin: 'casual_friday', storeAvailability: 'independent', metadata: { status: 'complete' }, metadataReviewedBy: actor._id, metadataReviewedAt: new Date() }); } else { const title = cleanDisplayTitle(data.title); if (!title) throw new AppError(400, 'invalid_request', 'title is required'); game = await CanonicalGame.create({ canonicalTitle: title, normalizedTitle: normalizeTitle(title), artwork: data.artworkOverride, summary: data.info, origin: 'casual_friday', storeAvailability: 'independent', metadata: { status: 'complete' }, metadataReviewedBy: actor._id, metadataReviewedAt: new Date() }); } return createRotation(actor, { ...data, canonicalGameId: game._id, displayTitle: data.displayTitle || cleanDisplayTitle(game.canonicalTitle), info: data.info ?? game.summary }, { itadClient }); }
async function updateRotation(actor, id, data, { itadClient } = {}) { const rotation = await Rotation.findById(id); if (!rotation) throw new AppError(404, 'not_found', 'Rotation game was not found'); const identity = `${rotation.displayTitle}:${rotation.acquisitionKind}`; Object.assign(rotation, fields({ ...rotation.toObject(), ...data }), { updatedBy: actor._id }); if (`${rotation.displayTitle}:${rotation.acquisitionKind}` !== identity) await verifyItad(rotation, itadClient); await rotation.save(); await audit(actor, 'rotation_updated', { rotationGameId: rotation._id }); return rotationDto(rotation, await CanonicalGame.findById(rotation.canonicalGameId)); }
async function recheckItad(actor, id, { itadClient } = {}) { const rotation = await Rotation.findById(id); if (!rotation) throw new AppError(404, 'not_found', 'Rotation game was not found'); await verifyItad(rotation, itadClient); rotation.updatedBy = actor._id; await rotation.save(); await audit(actor, 'rotation_itad_rechecked', { rotationGameId: rotation._id }); return rotationDto(rotation, await CanonicalGame.findById(rotation.canonicalGameId)); }
async function retireRotation(actor, id, reason) { const retiredAt = new Date(); const rotation = await Rotation.findOneAndUpdate({ _id: id, status: { $ne: 'retired' } }, { $set: { status: 'retired', retiredAt, retiredBy: actor._id, retirementReason: reason, updatedBy: actor._id } }, { new: true, runValidators: false }); if (!rotation) { if (!await Rotation.exists({ _id: id })) throw new AppError(404, 'not_found', 'Rotation game was not found'); return; } await audit(actor, 'rotation_retired', { rotationGameId: rotation._id, details: { reason } }); }
function nextFridayWindow(now = new Date()) { const day = now.getUTCDay(); const days = ((5 - day + 7) % 7) || 7; const friday = new Date(Date.UTC(now.getUTCFullYear(), now.getUTCMonth(), now.getUTCDate() + days)); const weekKey = friday.toISOString().slice(0, 10); const startsAt = new Date(`${weekKey}T19:00:00.000Z`); return { weekKey, startsAt, endsAt: new Date(startsAt.getTime() + 9 * 3600000) }; }
async function upcomingPlaylist(actor) { const window = nextFridayWindow(); const existing = await Playlist.findOne({ weekKey: window.weekKey }); return existing || Playlist.create({ ...window, createdBy: actor._id, updatedBy: actor._id }); }
async function buildPlaylistDto(playlist, userId, { itadClient, includeOffers = false } = {}) { if (!playlist) return null; const entries = await Entry.find({ playlistId: playlist._id }).sort({ position: 1 }); const canonicalGameIds = entries.map((entry) => entry.canonicalGameId); const ownership = userId ? await LibraryItem.find({ userId, canonicalGameId: { $in: canonicalGameIds }, removedAt: null }).select('canonicalGameId provider') : []; const providers = new Map(); ownership.forEach((item) => providers.set(String(item.canonicalGameId), [...new Set([...(providers.get(String(item.canonicalGameId)) || []), item.provider])])); const rotations = includeOffers ? await Rotation.find({ _id: { $in: entries.map((entry) => entry.rotationGameId) } }).select('itadStatus itadGameId') : []; const rotationMap = new Map(rotations.map((rotation) => [String(rotation._id), rotation])); const offers = new Map(); if (includeOffers && itadClient) await Promise.all(rotations.map(async (rotation) => { if (rotation.itadStatus !== 'verified' || !rotation.itadGameId) return; try { offers.set(String(rotation._id), await itadClient.bestOffer(rotation.itadGameId)); } catch { offers.set(String(rotation._id), null); } })); return { id: String(playlist._id), weekKey: playlist.weekKey, status: playlist.status, startsAt: playlist.startsAt, endsAt: playlist.endsAt, version: playlist.version, entries: entries.map((entry) => { const rotation = rotationMap.get(String(entry.rotationGameId)); return { id: String(entry._id), rotationGameId: String(entry.rotationGameId), canonicalGameId: String(entry.canonicalGameId), position: entry.position, ...entry.snapshots, owned: Boolean(providers.get(String(entry.canonicalGameId))?.length), providers: providers.get(String(entry.canonicalGameId)) || [], itad: includeOffers ? { status: rotation?.itadStatus || 'unknown', offer: offers.get(String(entry.rotationGameId)) || null } : undefined }; }) }; }
async function addToPlaylist(actor, id, { itadClient } = {}) { const rotation = await Rotation.findOne({ _id: id, status: 'active' }); if (!rotation) throw new AppError(404, 'not_found', 'Active rotation game was not found'); if (!playable(rotation)) throw new AppError(409, 'itad_verification_required', 'Verify ITAD or supply a free/web acquisition link before adding this game'); const playlist = await upcomingPlaylist(actor); if (await Entry.exists({ playlistId: playlist._id, rotationGameId: rotation._id })) return buildPlaylistDto(playlist, actor._id, { itadClient, includeOffers: true }); const count = await Entry.countDocuments({ playlistId: playlist._id }); const game = await CanonicalGame.findById(rotation.canonicalGameId); await Entry.create({ playlistId: playlist._id, rotationGameId: rotation._id, canonicalGameId: game._id, position: count + 1, selectedBy: actor._id, snapshots: { game: gameDto(game), rotation: { displayTitle: rotation.displayTitle, artwork: rotation.artworkOverride || game.artwork, info: rotation.info || game.summary, playerCountMin: rotation.playerCountMin, playerCountMax: rotation.playerCountMax, playerCountLabel: rotation.playerCountLabel, joinInstructions: rotation.joinInstructions, hostMode: rotation.hostMode, acquisitionKind: rotation.acquisitionKind, acquisitionUrl: rotation.acquisitionUrl, availabilityNote: rotation.availabilityNote } } }); playlist.version += 1; playlist.updatedBy = actor._id; await playlist.save(); await audit(actor, 'playlist_entry_added', { playlistId: playlist._id, rotationGameId: rotation._id, afterVersion: playlist.version }); return buildPlaylistDto(playlist, actor._id, { itadClient, includeOffers: true }); }
async function removeFromPlaylist(actor, playlistId, entryId, version, { itadClient } = {}) { const playlist = await Playlist.findOneAndUpdate({ _id: playlistId, status: 'draft', version }, { $inc: { version: 1 }, $set: { updatedBy: actor._id } }, { new: true }); if (!playlist) { const existing = await Playlist.findById(playlistId); if (!existing) throw new AppError(404, 'not_found', 'Playlist was not found'); if (existing.status !== 'draft') throw new AppError(409, 'playlist_not_editable', 'Only a draft playlist can be changed'); throw new AppError(409, 'playlist_version_conflict', 'This playlist changed. Reload it before saving.'); } const entry = await Entry.findOneAndDelete({ _id: entryId, playlistId: playlist._id }); if (!entry) { await Playlist.updateOne({ _id: playlist._id, version: playlist.version }, { $inc: { version: -1 } }); throw new AppError(404, 'not_found', 'Playlist entry was not found'); } await Entry.updateMany({ playlistId: playlist._id, position: { $gt: entry.position } }, { $inc: { position: -1 } }); await audit(actor, 'playlist_entry_removed', { playlistId: playlist._id, rotationGameId: entry.rotationGameId, beforeVersion: version, afterVersion: playlist.version }); return buildPlaylistDto(playlist, actor._id, { itadClient, includeOffers: true }); }
async function reorderPlaylist(actor, playlistId, entryIds, version, { itadClient } = {}) { const entries = await Entry.find({ playlistId }).sort({ position: 1 }); const currentIds = entries.map((entry) => String(entry._id)); if (entryIds.length !== currentIds.length || new Set(entryIds).size !== entryIds.length || entryIds.some((entryId) => !currentIds.includes(entryId))) throw new AppError(400, 'invalid_playlist_order', 'entryIds must contain every playlist entry exactly once'); const playlist = await Playlist.findOneAndUpdate({ _id: playlistId, status: 'draft', version }, { $inc: { version: 1 }, $set: { updatedBy: actor._id } }, { new: true }); if (!playlist) { const existing = await Playlist.findById(playlistId); if (!existing) throw new AppError(404, 'not_found', 'Playlist was not found'); if (existing.status !== 'draft') throw new AppError(409, 'playlist_not_editable', 'Only a draft playlist can be changed'); throw new AppError(409, 'playlist_version_conflict', 'This playlist changed. Reload it before saving.'); } await Entry.bulkWrite(entryIds.map((entryId, index) => ({ updateOne: { filter: { _id: entryId, playlistId }, update: { $set: { position: -(index + 1) } } } })), { ordered: true }); await Entry.bulkWrite(entryIds.map((entryId, index) => ({ updateOne: { filter: { _id: entryId, playlistId }, update: { $set: { position: index + 1 } } } })), { ordered: true }); await audit(actor, 'playlist_reordered', { playlistId: playlist._id, beforeVersion: version, afterVersion: playlist.version, details: { entryIds } }); return buildPlaylistDto(playlist, actor._id, { itadClient, includeOffers: true }); }
async function publishPlaylist(actor, id, version, { itadClient } = {}) { const playlist = await Playlist.findById(id); if (!playlist) throw new AppError(404, 'not_found', 'Playlist was not found'); if (playlist.version !== version) throw new AppError(409, 'playlist_version_conflict', 'This playlist changed. Reload it before saving.'); const count = await Entry.countDocuments({ playlistId: playlist._id }); if (count < 1) throw new AppError(400, 'invalid_playlist_size', 'A playlist needs at least one game before publication'); Object.assign(playlist, { status: 'published', version: playlist.version + 1, publishedBy: actor._id, publishedAt: new Date(), updatedBy: actor._id }); await playlist.save(); await audit(actor, 'playlist_published', { playlistId: playlist._id, afterVersion: playlist.version }); return buildPlaylistDto(playlist, actor._id, { itadClient, includeOffers: true }); }
async function completeElapsedPlaylists(now = new Date()) { return (await Playlist.updateMany({ status: 'published', endsAt: { $lte: now } }, { $set: { status: 'completed', completedAt: now }, $inc: { version: 1 } })).modifiedCount; }
module.exports = { addToPlaylist, buildPlaylistDto, cleanDisplayTitle, completeElapsedPlaylists, createExternalRotation, createRotation, listRotation, nextFridayWindow, publishPlaylist, recheckItad, removeFromPlaylist, reorderPlaylist, retireRotation, updateRotation };