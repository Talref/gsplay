import { useCallback, useEffect, useRef, useState } from 'react';
import { Alert, Button, Card, CardContent, CircularProgress, Divider, Snackbar, Stack, TextField, Typography } from '@mui/material';
import { Navigate } from 'react-router-dom';
import { adminApi } from '../services/api';
import { useAuth } from '../context/useAuth';
import CatalogueGameSearch from '../components/CatalogueGameSearch';

const splitList = (value) => value.split(',').map((item) => item.trim()).filter(Boolean);
const blank = { title: '', summary: '', artwork: '', genres: '', platforms: '', releaseDate: '' };

function MetadataFields({ value, onChange }) {
  const change = (field) => (event) => onChange({ ...value, [field]: event.target.value });
  return <Stack spacing={1.5} sx={{ mt: 2, mb: 2 }}><TextField required fullWidth label="Game title" value={value.title} onChange={change('title')} /><TextField fullWidth multiline minRows={2} label="Summary" value={value.summary} onChange={change('summary')} /><TextField fullWidth label="Artwork URL" value={value.artwork} onChange={change('artwork')} /><Stack direction={{ xs: 'column', sm: 'row' }} spacing={1}><TextField fullWidth label="Genres (comma-separated)" value={value.genres} onChange={change('genres')} /><TextField fullWidth label="Platforms (comma-separated)" value={value.platforms} onChange={change('platforms')} /><TextField type="date" fullWidth label="Release date" InputLabelProps={{ shrink: true }} value={value.releaseDate} onChange={change('releaseDate')} /></Stack></Stack>;
}

export default function AdminCatalogue() {
  const { user } = useAuth();
  const [version, setVersion] = useState(0); const [reviews, setReviews] = useState([]); const [reviewPage, setReviewPage] = useState({ number: 0, size: 30, total: 0 }); const [reviewLoading, setReviewLoading] = useState(true); const reviewSentinel = useRef(null);
  const [notice, setNotice] = useState(''); const [error, setError] = useState(''); const [manual, setManual] = useState(blank); const [igdbUrl, setIgdbUrl] = useState(''); const [selected, setSelected] = useState(null); const [draft, setDraft] = useState(blank); const [selectedIgdbUrl, setSelectedIgdbUrl] = useState(''); const [mergeTarget, setMergeTarget] = useState(null); const [reason, setReason] = useState(''); const [identities, setIdentities] = useState([]); const [selectedIdentity, setSelectedIdentity] = useState(null); const [reviewUrls, setReviewUrls] = useState({}); const [manualReviewId, setManualReviewId] = useState(null); const [manualReview, setManualReview] = useState(blank);
  const selectedId = selected?.id; const targetId = mergeTarget?.id;
  const refresh = () => setVersion((current) => current + 1); const report = (message) => { setNotice(message); setError(''); };
  const loadReviews = useCallback(async (nextPage, replace = false) => { setReviewLoading(true); try { const response = await adminApi.metadataReviews(nextPage, reviewPage.size); setReviews((current) => replace ? response.reviews : [...current, ...response.reviews]); setReviewPage(response.page); } catch (err) { setError(err.message); } finally { setReviewLoading(false); } }, [reviewPage.size]);
  useEffect(() => { loadReviews(1, true); }, [loadReviews, version]);
  const hasMoreReviews = reviews.length < reviewPage.total;
  useEffect(() => { const target = reviewSentinel.current; if (!target || !hasMoreReviews || reviewLoading) return undefined; const observer = new IntersectionObserver((entries) => { if (entries[0].isIntersecting) loadReviews(reviewPage.number + 1); }, { rootMargin: '280px' }); observer.observe(target); return () => observer.disconnect(); }, [hasMoreReviews, loadReviews, reviewLoading, reviewPage.number]);
  const select = (game) => { setSelected(game || null); setSelectedIgdbUrl(''); setMergeTarget(null); setReason(''); setIdentities([]); setSelectedIdentity(null); setDraft(game ? { title: game.title, summary: game.summary || '', artwork: game.artwork || '', genres: (game.genres || []).join(', '), platforms: (game.platforms || []).join(', '), releaseDate: game.releaseDate ? game.releaseDate.slice(0, 10) : '' } : blank); };
  useEffect(() => { if (!selectedId) return undefined; let active = true; adminApi.providerIdentities(selectedId).then((response) => { if (active) setIdentities(response.identities); }).catch((err) => { if (active) setError(err.message); }); return () => { active = false; }; }, [selectedId, version]);
  const save = async () => { try { const result = await adminApi.updateGame(selectedId, { ...draft, genres: splitList(draft.genres), platforms: splitList(draft.platforms), releaseDate: draft.releaseDate || null }); report(`Successfully updated ${result.game.title}.`); refresh(); } catch (err) { setError(err.message); } };
  const addManual = async () => { try { const result = await adminApi.createGame({ ...manual, genres: splitList(manual.genres), platforms: splitList(manual.platforms), releaseDate: manual.releaseDate || undefined, independent: true }); report(`${result.game.title} added with the supplied metadata.`); setManual(blank); refresh(); } catch (err) { setError(err.message); } };
  const addIgdb = async () => { try { const result = await adminApi.createFromIgdbUrl(igdbUrl); report(result.created ? `${result.game.title} was imported from verified IGDB metadata.` : `${result.game.title} already exists; no duplicate was created.`); setIgdbUrl(''); refresh(); } catch (err) { setError(err.message); } };
  const attachIgdbUrl = async () => { try { const result = await adminApi.assignIgdbUrl(selectedId, selectedIgdbUrl); select(result.game); report(resolutionMessage(result, selected.title)); refresh(); } catch (err) { setError(err.message); } };
  const refreshSelectedMetadata = async () => { try { const result = await adminApi.refreshGameMetadata(selectedId); report(result.coalesced ? `An IGDB refresh for ${selected.title} is already queued.` : `IGDB metadata refresh queued for ${selected.title}.`); refresh(); } catch (err) { setError(err.message); } };
  const removeReview = (id) => setReviews((current) => current.filter((review) => review.game.id !== id));
  const resolutionMessage = (result, sourceTitle) => result.merged ? `${sourceTitle} was merged into ${result.game.title} based on the verified IGDB identity.` : `Verified IGDB metadata attached to ${result.game.title}.`;
  const resolveReviewUrl = async (id) => { try { const result = await adminApi.assignIgdbUrl(id, reviewUrls[id]); report(resolutionMessage(result, reviews.find((review) => review.game.id === id)?.game.title || 'Game')); removeReview(id); } catch (err) { setError(err.message); } };
  const resolveReviewManual = async (id) => { try { const result = await adminApi.manualMetadata(id, { ...manualReview, genres: splitList(manualReview.genres), platforms: splitList(manualReview.platforms), releaseDate: manualReview.releaseDate || null }); report(`Manual metadata saved for ${result.game.title}.`); setManualReviewId(null); removeReview(id); } catch (err) { setError(err.message); } };
  const hide = async (id) => { try { const result = await adminApi.setVisibility(id, true); report(`Successfully hid ${result.game.title} from the catalogue.`); if (selectedId === id) select(null); removeReview(id); } catch (err) { setError(err.message); } };
  const unhide = async () => { try { const result = await adminApi.setVisibility(selectedId, false); select(result.game); report(`${result.game.title} is visible again.`); refresh(); } catch (err) { setError(err.message); } };
  const choose = async (gameId, igdbId) => { try { const result = await adminApi.assignIgdb(gameId, igdbId); report(resolutionMessage(result, reviews.find((review) => review.game.id === gameId)?.game.title || 'Game')); removeReview(gameId); } catch (err) { setError(err.message); } };
  const merge = async () => { try { await adminApi.mergeGames(selectedId, targetId, reason || 'Admin catalogue consolidation'); report('Games merged; entitlements and aliases now reference the survivor.'); select(null); refresh(); } catch (err) { setError(err.message); } };
  const reassign = async () => { if (!selectedIdentity || !targetId) return; if (!window.confirm(`Move ${selectedIdentity.provider} ${selectedIdentity.providerGameId} for ${selectedIdentity.affectedUserCount} active owner(s) to ${mergeTarget.title}?`)) return; try { const result = await adminApi.reassignProviderGame(selectedId, { provider: selectedIdentity.provider, providerGameId: selectedIdentity.providerGameId, targetGameId: targetId, confirmation: 'REASSIGN PROVIDER GAME', reason: reason || 'Admin correction of a collapsed catalogue record' }); report(`Moved ${result.activeEntitlementCount} entitlement(s) across ${result.affectedUserCount} owner(s) to ${result.targetGame.title}. Future syncs will preserve this correction.`); setSelectedIdentity(null); setMergeTarget(null); refresh(); } catch (err) { setError(err.message); } };
  const archive = async () => { if (!window.confirm('Archive this unreferenced game? Referenced games must be merged instead.')) return; try { await adminApi.archiveGame(selectedId, reason); report('Unreferenced catalogue record archived.'); select(null); refresh(); } catch (err) { setError(err.message); } };
  if (user?.role !== 'admin') return <Navigate to="/" />;
  return <><Stack spacing={3}><Typography variant="h2">Catalogue stewardship</Typography><Typography color="text.secondary">Edit bad titles, refresh verified IGDB metadata, safely merge duplicates, and repair collapsed provider identities without asking members to do anything.</Typography>{error && <Alert severity="error">{error}</Alert>}<Card><CardContent><Typography variant="h6">Add from IGDB link</Typography><Stack direction={{ xs: 'column', sm: 'row' }} spacing={1} sx={{ mt: 2 }}><TextField fullWidth label="IGDB game URL" placeholder="https://www.igdb.com/games/vintage-story" value={igdbUrl} onChange={(event) => setIgdbUrl(event.target.value)} /><Button variant="contained" disabled={!igdbUrl.trim()} onClick={addIgdb}>Import verified game</Button></Stack></CardContent></Card><Card><CardContent><Typography variant="h6">Add manually</Typography><MetadataFields value={manual} onChange={setManual} /><Button variant="outlined" disabled={!manual.title.trim()} onClick={addManual}>Add independent PC game</Button></CardContent></Card><Card><CardContent><Typography variant="h6">Find and correct a catalogue game</Typography><Typography color="text.secondary" sx={{ mt: 1, mb: 2 }}>Searches active and hidden catalogue records on the server. Preview appears after three characters.</Typography><CatalogueGameSearch label="Search catalogue" onSelect={select} loadGames={adminApi.games} />{selected && <Stack spacing={2} sx={{ mt: 2 }}><Typography color="text.secondary">Editing: {selected.title} · Status: {selected.metadataStatus}{selected.hidden ? ' · Hidden' : ''}</Typography><Stack direction={{ xs: 'column', sm: 'row' }} spacing={1}><TextField fullWidth label="Correct IGDB game URL" placeholder="https://www.igdb.com/games/correct-game" value={selectedIgdbUrl} onChange={(event) => setSelectedIgdbUrl(event.target.value)} helperText="Pulls verified IGDB metadata into this record. Ownership is unchanged." /><Button variant="outlined" disabled={!selectedIgdbUrl.trim()} onClick={attachIgdbUrl}>Pull verified metadata</Button></Stack><Button variant="outlined" onClick={refreshSelectedMetadata}>Refresh this game from IGDB</Button><Typography variant="caption" color="text.secondary">Queues a background refresh for this one game. It keeps ownership unchanged and uses its verified IGDB ID when available.</Typography><MetadataFields value={draft} onChange={setDraft} /><Stack direction={{ xs: 'column', sm: 'row' }} spacing={1}><Button variant="contained" onClick={save}>Save changes</Button>{selected.hidden ? <Button color="success" variant="outlined" onClick={unhide}>Unhide</Button> : <Button color="warning" variant="outlined" onClick={() => hide(selectedId)}>Hide from Catalogue</Button>}</Stack><Divider /><Typography variant="h6">Repair a collapsed provider game</Typography><Typography color="text.secondary">Choose the exact provider ID to move. Every current owner moves to the target, and future syncs follow the correction.</Typography><Stack direction="row" flexWrap="wrap" gap={1}>{identities.map((identity) => <Button key={`${identity.provider}:${identity.providerGameId}`} variant={selectedIdentity?.provider === identity.provider && selectedIdentity?.providerGameId === identity.providerGameId ? 'contained' : 'outlined'} onClick={() => setSelectedIdentity(identity)}>{identity.provider} · {identity.providerGameId} · {identity.providerTitles.join(' / ')} · {identity.affectedUserCount} owners</Button>)}</Stack>{selectedIdentity && <><CatalogueGameSearch label="Search correct destination game" excludeId={selectedId} onSelect={setMergeTarget} loadGames={adminApi.games} />{mergeTarget && <Typography color="text.secondary">Move {selectedIdentity.provider} {selectedIdentity.providerGameId} to: {mergeTarget.title}</Typography>}<TextField required label="Repair reason" value={reason} onChange={(event) => setReason(event.target.value)} helperText="This writes a durable audit record." /><Button color="warning" variant="contained" disabled={!targetId || !reason.trim()} onClick={reassign}>Move this provider game for every owner</Button></>}<Divider /><CatalogueGameSearch label="Search surviving game to merge into" excludeId={selectedId} onSelect={setMergeTarget} loadGames={adminApi.games} />{mergeTarget && <Typography color="text.secondary">Merge target: {mergeTarget.title}</Typography>}<TextField label="Merge/archive reason" value={reason} onChange={(event) => setReason(event.target.value)} /><Stack direction={{ xs: 'column', sm: 'row' }} spacing={1}><Button variant="outlined" disabled={!targetId} onClick={merge}>Merge into survivor</Button><Button color="error" variant="outlined" onClick={archive}>Archive unreferenced game</Button></Stack></Stack>}</CardContent></Card><Card><CardContent><Typography variant="h6">IGDB ambiguity review</Typography><Typography color="text.secondary" sx={{ mt: 1 }}>Every failed, visible catalogue record. Resolve one at a time; only the three strongest suggestions are shown.</Typography><Divider sx={{ my: 1 }} />{reviews.map((review) => <Stack key={review.game.id} spacing={1.5} sx={{ py: 2, borderBottom: 1, borderColor: 'divider' }}><Typography variant="h6">{review.game.title}</Typography><Typography color="text.secondary">{review.error}</Typography><Stack direction="row" flexWrap="wrap" gap={1}>{review.candidates.map((candidate) => <Button key={candidate.igdbId} variant="outlined" onClick={() => choose(review.game.id, candidate.igdbId)}>Use {candidate.title}</Button>)}</Stack><Stack direction={{ xs: 'column', sm: 'row' }} spacing={1}><TextField fullWidth label="IGDB game URL" placeholder="https://www.igdb.com/games/example" value={reviewUrls[review.game.id] || ''} onChange={(event) => setReviewUrls({ ...reviewUrls, [review.game.id]: event.target.value })} /><Button variant="outlined" disabled={!reviewUrls[review.game.id]?.trim()} onClick={() => resolveReviewUrl(review.game.id)}>Enrich with IGDB Link</Button></Stack><Stack direction={{ xs: 'column', sm: 'row' }} spacing={1}><Button variant="outlined" onClick={() => { setManualReviewId(manualReviewId === review.game.id ? null : review.game.id); setManualReview({ title: review.game.title, summary: review.game.summary || '', artwork: review.game.artwork || '', genres: (review.game.genres || []).join(', '), platforms: (review.game.platforms || []).join(', '), releaseDate: review.game.releaseDate ? review.game.releaseDate.slice(0, 10) : '' }); }}>Enrich manually</Button><Button color="warning" variant="outlined" onClick={() => hide(review.game.id)}>Hide from Catalogue</Button></Stack>{manualReviewId === review.game.id && <><MetadataFields value={manualReview} onChange={setManualReview} /><Button variant="contained" disabled={!manualReview.title.trim()} onClick={() => resolveReviewManual(review.game.id)}>Save manual metadata</Button></>}</Stack>)}{reviewLoading && <CircularProgress aria-label="Loading failed games" />}{hasMoreReviews && <div ref={reviewSentinel} aria-label="Load more failed games" />}{!reviewLoading && !reviews.length && <Typography color="text.secondary">No failed visible games await review.</Typography>}</CardContent></Card></Stack><Snackbar open={Boolean(notice)} autoHideDuration={4200} onClose={() => setNotice('')} anchorOrigin={{ vertical: 'bottom', horizontal: 'center' }}><Alert severity="success" variant="filled" onClose={() => setNotice('')}>{notice}</Alert></Snackbar><Snackbar open={Boolean(error)} autoHideDuration={6000} onClose={() => setError('')} anchorOrigin={{ vertical: 'bottom', horizontal: 'center' }}><Alert severity="error" variant="filled" onClose={() => setError('')}>{error}</Alert></Snackbar></>;
}