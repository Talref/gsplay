import { useCallback, useEffect, useRef, useState } from 'react';
import { Alert, Button, Card, CardContent, CircularProgress, Grid, IconButton, MenuItem, Stack, TextField, Tooltip, Typography } from '@mui/material';
import { Link } from 'react-router-dom';
import InfoOutlinedIcon from '@mui/icons-material/InfoOutlined';
import { useAuth } from '../context/useAuth';
import { libraryApi } from '../services/api';
import ProviderIcons from '../components/ProviderIcons';
import ThemedDialog from '../components/ThemedDialog';

export default function Library() {
  const { user, refresh } = useAuth(); const [items, setItems] = useState([]); const [page, setPage] = useState({ number: 0, size: 60, total: 0 }); const [loading, setLoading] = useState(true); const [libraryError, setLibraryError] = useState(''); const [steamId, setSteamId] = useState(''); const [editingSteam, setEditingSteam] = useState(false); const [provider, setProvider] = useState('gog'); const [file, setFile] = useState(null); const [message, setMessage] = useState(''); const [jobId, setJobId] = useState(null); const [job, setJob] = useState(null); const [help, setHelp] = useState(null); const [removeItem, setRemoveItem] = useState(null); const loadMoreRef = useRef(null);
  const loadLibrary = useCallback(async (nextPage, replace = false) => { setLoading(true); setLibraryError(''); try { const response = await libraryApi.mine(nextPage, page.size); setItems((current) => replace ? response.items : [...current, ...response.items]); setPage(response.page); } catch (error) { setLibraryError(error.message); } finally { setLoading(false); } }, [page.size]);
  useEffect(() => { loadLibrary(1, true); }, [loadLibrary]);
  useEffect(() => { if (!jobId || ['completed', 'completed_with_errors', 'failed'].includes(job?.status)) return undefined; const poll = async () => { try { const result = await libraryApi.job(jobId); setJob(result.job); if (['completed', 'completed_with_errors'].includes(result.job.status)) { await refresh(); await loadLibrary(1, true); } } catch (error) { setMessage(error.message); } }; poll(); const timer = setInterval(poll, 2000); return () => clearInterval(timer); }, [jobId, job?.status, loadLibrary, refresh]);
  const run = async (action) => { try { const result = await action(); if (result?.job) { setJobId(result.job.id); setJob({ ...result.job, status: result.job.status || 'queued' }); setMessage(`Coda fatta${result.job.gameCount ? `: ${result.job.gameCount} giochi ar macero` : ''}.`); } else { setMessage('Salvato. Mica pizza e fichi.'); await refresh(); setSteamId(''); setEditingSteam(false); } } catch { setMessage('Aò, qualcosa s’è incartato. Riprova tra poco.'); } };
  const jobNotice = job && <Alert severity={job.status === 'failed' ? 'error' : job.status === 'completed_with_errors' ? 'warning' : job.status === 'completed' ? 'success' : 'info'}>{job.status === 'completed' ? 'Fatto, daje.' : job.status === 'failed' ? 'È annata male, porca paletta.' : 'Stamo a lavoracce sopra…'}{job.counts?.discovered !== undefined ? ` · ${job.counts.discovered} trovati, ${job.counts.created} aggiunti, ${job.counts.updated} aggiornati` : ''}</Alert>;
  const hasMore = items.length < page.total;
  useEffect(() => { const target = loadMoreRef.current; if (!target || !hasMore || loading) return undefined; const observer = new IntersectionObserver((entries) => { if (entries[0].isIntersecting) loadLibrary(page.number + 1); }, { rootMargin: '280px' }); observer.observe(target); return () => observer.disconnect(); }, [hasMore, loading, loadLibrary, page.number]);
  const steamHelp = <><Typography>Per Trovare il tuo steamID:</Typography><ol><li>Visita il tuo profilo Steam (log in se necessario)</li><li>Il tuo SteamID sono le ultime 17 cifre dell’URL</li><li>Se il tuo profilo steam non finisce con lo SteamID visita SteamID.io ed incolla il link del tuo profilo</li><li>SteamID.io calcolera' il tuo SteamID dal link (quello corretto e' SteamID64).</li><li>Inserisci il tuo SteamID nel form di questa pagina</li><li>ATTENZIONE: La tua lista di giochi dev'essere PUBBLICA sul tuo profilo Steam! Altrimenti l'app non puo' leggerla!</li></ol><Typography>Non c'e' bisogno di ripetere questa operazione. Se aggiungete nuovi giochi alla vostra libreria basta cliccare su Aggiorna Libreria.</Typography></>;
  const importHelp = <><ol><li>Installa Heroic Games Launcher</li><li>Collega i tuoi account e popola la tua libreria</li><li>Apri: C:\Users\NOME_UTENTE\AppData\Roaming\heroic\store_cache (Windows) o: ~/.config/heroic/store_cache/ (Linux) (attenzione, i percorsi derivano da test, se avete i file in un altra posizione pingatemi sul server - @eradan)</li><li>Carica i file rilevanti che finiscono in "library" cliccando su "Importa GOG/Epic" (gog_library.json per GOG, legendary_library.json per Epic e nile_library.json per Amazon Games)</li></ol><Typography>Fatto! Ricorda di ricaricare i file ogni tanto se aggiungi nuovi giochi!</Typography></>;
  const removeManual = async () => { try { await libraryApi.removeManualGame(removeItem.canonicalGame.id); setRemoveItem(null); setMessage('Le aggiunta manuale è stata levata dalla libbreria.'); await loadLibrary(1, true); } catch (error) { setMessage(error.message); } };
  return <Stack spacing={3}><Typography variant="h2">La tua libbreria</Typography>{message && <Alert severity="info">{message}</Alert>}{jobNotice}{libraryError && <Alert severity="error">Aò, la libbreria s’è impicciata: riprova tra poco.</Alert>}<Grid container spacing={2} className="equal-height-grid"><Grid size={{ xs: 12, lg: 6 }}><Card><CardContent><Stack direction="row" alignItems="center"><Typography variant="h6">Collegamento Steam</Typography><Tooltip title="Come se fa?"><IconButton color="primary" onClick={() => setHelp('steam')} aria-label="Istruzioni SteamID"><InfoOutlinedIcon /></IconButton></Tooltip></Stack><Stack direction={{ xs: 'column', sm: 'row' }} spacing={1} sx={{ mt: 2 }}>{user?.steamAccount?.steamId && !editingSteam ? <Typography color="primary" sx={{ flexGrow: 1, alignSelf: 'center' }}>SteamID a posto, daje.</Typography> : <TextField fullWidth label="Aggiungi SteamID" value={steamId} onChange={(event) => setSteamId(event.target.value)} />}<Button variant="outlined" disabled={!editingSteam && !steamId && !user?.steamAccount?.steamId} onClick={() => editingSteam || !user?.steamAccount?.steamId ? run(() => libraryApi.linkSteam(steamId)) : setEditingSteam(true)}>{user?.steamAccount?.steamId && !editingSteam ? 'Cambia SteamID' : 'Salva'}</Button><Button variant="contained" disabled={!user?.steamAccount?.steamId} onClick={() => run(libraryApi.syncSteam)}>Daje co’ Steam</Button></Stack></CardContent></Card></Grid><Grid size={{ xs: 12, lg: 6 }}><Card><CardContent><Stack direction="row" alignItems="center"><Typography variant="h6">Importa la libbreria</Typography><Tooltip title="Che file ce vogliono?"><IconButton color="primary" onClick={() => setHelp('import')} aria-label="Istruzioni importazione"><InfoOutlinedIcon /></IconButton></Tooltip></Stack><Stack direction={{ xs: 'column', sm: 'row' }} spacing={1} sx={{ mt: 2 }}><TextField select label="Piattaforma" value={provider} onChange={(event) => setProvider(event.target.value)}>{['gog', 'epic', 'amazon'].map((value) => <MenuItem key={value} value={value}><Stack direction="row" spacing={1} alignItems="center"><ProviderIcons providers={[value]} /> <span>{value.toUpperCase()}</span></Stack></MenuItem>)}</TextField><Button component="label" variant="outlined" sx={{ minWidth: 180 }}>{file?.name || 'Scegli CSV / JSON'}<input hidden type="file" accept=".csv,.json,text/csv,application/json" onChange={(event) => setFile(event.target.files?.[0])} /></Button><Button variant="contained" disabled={!file} onClick={() => run(() => libraryApi.upload(provider, file))}>Manda in coda</Button></Stack></CardContent></Card></Grid></Grid>{page.total > 0 && <Typography color="text.secondary">{items.length} giochi. Roba seria, insomma.</Typography>}<Grid container spacing={2}>{items.map((item) => { const title = item.canonicalGame?.title || item.providerTitle; const artwork = item.canonicalGame?.artwork; const manual = item.providers.includes('manual') && item.providers.length === 1; return <Grid key={item.id} size={{ xs: 12, sm: 6, xl: 4 }}><Card className={`game-card${artwork ? ' game-card--cover' : ''}`} sx={{ display: 'block', backgroundImage: artwork ? `url(${JSON.stringify(artwork)})` : undefined }}><CardContent><Stack spacing={1}><Stack direction="row" justifyContent="space-between" spacing={1} alignItems="flex-start"><Typography component={item.canonicalGame ? Link : 'span'} to={item.canonicalGame ? `/catalogue/${item.canonicalGame.id}` : undefined} variant="h6" className="game-title-clamp" sx={{ flexGrow: 1, color: 'inherit', textDecoration: 'none' }}>{title}</Typography><ProviderIcons providers={item.providers} /></Stack>{manual && <Button color="error" size="small" onClick={() => setRemoveItem(item)} sx={{ alignSelf: 'flex-start' }}>Rimuovi dalla libbreria</Button>}</Stack></CardContent></Card></Grid>; })}</Grid>{loading && <CircularProgress aria-label="Caricamento giochi" />}{hasMore && <div ref={loadMoreRef} aria-label="Carica altri giochi" />}{!loading && !items.length && <Typography color="text.secondary">Qua c’è più vuoto de Trastevere alle sette. Collegati Steam o importa un file.</Typography>}<ThemedDialog open={help === 'steam'} onClose={() => setHelp(null)} title="Come trovare lo SteamID">{steamHelp}</ThemedDialog><ThemedDialog open={help === 'import'} onClose={() => setHelp(null)} title="Come importare giochi da GOG/Epic/Amazon Games">{importHelp}</ThemedDialog><ThemedDialog open={Boolean(removeItem)} onClose={() => setRemoveItem(null)} title="Leva dalla libbreria?"><Stack spacing={2}><Typography>Rimuoverò solo l’aggiunta manuale di <strong>{removeItem?.canonicalGame?.title}</strong>.</Typography><Stack direction="row" justifyContent="flex-end" spacing={1}><Button onClick={() => setRemoveItem(null)}>Annulla</Button><Button color="error" variant="contained" onClick={removeManual}>Sì, rimuovi</Button></Stack></Stack></ThemedDialog></Stack>;
}